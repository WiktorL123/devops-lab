# GCP mapping skill

This is a future mapping reference, not an active implementation profile.

Likely mappings:
- Azure Container Apps -> Cloud Run
- Azure PostgreSQL -> Cloud SQL for PostgreSQL
- ACR -> Artifact Registry
- Key Vault -> Secret Manager
- Blob state -> Cloud Storage Terraform backend

Do not implement GCP unless explicitly requested.
Preserve the same lab scope when adapting.
