name: backend-ci

on:
  pull_request:
    paths:
      - "backend/**"
      - ".nvmrc"
      - ".github/workflows/backend-ci.yml"
  push:
    branches:
      - main
    paths:
      - "backend/**"
      - ".nvmrc"
      - ".github/workflows/backend-ci.yml"

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  ci:
    name: lint-test-build-images
    runs-on: ubuntu-latest
    timeout-minutes: 15
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc
          cache: npm
          cache-dependency-path: backend/package-lock.json

      - name: Install dependencies
        run: npm ci

      - name: Generate Prisma Client
        run: npm run prisma:generate

      - name: Lint
        run: npm run lint

      - name: Test
        run: npm test

      - name: Build
        run: npm run build

      - name: Validate backend runtime container image
        run: docker build --target runtime --tag devops-lab-backend:ci .

      - name: Validate migration container image
        run: docker build --target migration --tag devops-lab-backend-migration:ci .

  publish-images:
    name: publish-images
    if: github.event_name == 'push'
    needs: ci
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: dev
    permissions:
      contents: read
      id-token: write
    env:
      ACR_NAME: ${{ vars.AZURE_CONTAINER_REGISTRY_NAME }}
    defaults:
      run:
        working-directory: backend

    steps:
      - name: Check out repository
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version-file: .nvmrc

      - name: Log in to Azure with the backend deployment identity
        uses: azure/login@93381592711f247e165c389ebb30b596c84cdc48 # v3.0.0
        with:
          client-id: ${{ vars.AZURE_CLIENT_ID_BACKEND_DEPLOY }}
          tenant-id: ${{ vars.AZURE_TENANT_ID }}
          subscription-id: ${{ vars.AZURE_SUBSCRIPTION_ID }}

      - name: Log in to Azure Container Registry
        run: az acr login --name "$ACR_NAME"

      - name: Compute immutable image tag
        id: image
        shell: bash
        run: |
          version="$(node -p "require('./package.json').version")"
          short_sha="${GITHUB_SHA::7}"
          echo "tag=${version}-${short_sha}" >> "$GITHUB_OUTPUT"

      - name: Build backend runtime image
        env:
          IMAGE_TAG: ${{ steps.image.outputs.tag }}
        run: docker build --target runtime --tag "$ACR_NAME.azurecr.io/backend:$IMAGE_TAG" .

      - name: Build backend migration image
        env:
          IMAGE_TAG: ${{ steps.image.outputs.tag }}
        run: docker build --target migration --tag "$ACR_NAME.azurecr.io/backend:migration-$IMAGE_TAG" .

      - name: Push backend images
        env:
          IMAGE_TAG: ${{ steps.image.outputs.tag }}
        run: |
          docker push "$ACR_NAME.azurecr.io/backend:$IMAGE_TAG"
          docker push "$ACR_NAME.azurecr.io/backend:migration-$IMAGE_TAG"
